#ifndef HTML_HPP
#define HTML_HPP
	#include "types.hpp"
	#include "common.hpp"
	
	namespace html {
		extern void renderuser (user*);
		extern void renderpost (post*);
		extern void renderminipost (post*);
	}
#endif
